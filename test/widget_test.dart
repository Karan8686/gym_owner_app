import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test — app renders', (tester) async {
    // Placeholder: real widget tests will be added per feature.
    expect(1 + 1, equals(2));
  });
}
